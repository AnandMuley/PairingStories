version '1.0'

apply plugin: 'java'
apply plugin: 'groovy'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile project(":lambda-collections")
    testCompile spock.core,spock.cglib,spock.objenesis, groovy
}
